import * as React from "react";
import styles from "./MyTasks.module.scss";

const MyTasks: React.FC = () => {
  return <div className={styles.mytasks_container}>MyTasks components ...</div>;
};

export default MyTasks;
